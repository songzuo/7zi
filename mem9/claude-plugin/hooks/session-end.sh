#!/usr/bin/env bash
# session-end.sh — Cleanup on session end.
# Hook: SessionEnd (sync, timeout: 5s)
# Currently a no-op placeholder. Can be used later for cleanup tasks
# like flushing pending memory writes or logging session stats.

exit 0
