# Changelog - v1.14.1

## 发布日期
2026-04-30

## 新增功能

### Tailwind CSS 配置增强

#### 1. CSS 变量映射
- 添加完整的 CSS 变量支持，使 Tailwind 工具类能够直接引用 `tokens.css` 中定义的设计变量
- 所有颜色、字体、阴影、圆角等现在都通过 CSS 变量进行动态切换
- 支持亮色/暗色主题无缝切换

#### 2. 字体族配置
- **Inter**: 主要无衬线字体，用于 UI 界面
- **JetBrains Mono**: 代码/等宽字体，用于代码展示

#### 3. 颜色系统
支持以下颜色分类的 CSS 变量映射：
- `primary`: 主色调 (50-900)
- `gray`: 灰色系 (50-900)
- `success`: 成功色 (50, 500-700)
- `warning`: 警告色 (50, 500-700)
- `error`: 错误色 (50, 500-700)
- `info`: 信息色 (50, 500-700)

#### 4. 圆角系统
新增自定义圆角配置：
- `none`: 0
- `sm`: 2px
- `DEFAULT`/`md`: 6px
- `lg`: 8px
- `xl`: 12px
- `2xl`: 16px
- `full`: 9999px (胶囊形状)

#### 5. 阴影系统
新增自定义阴影配置：
- `xs`: 极细阴影
- `sm`: 小阴影
- `DEFAULT`/`md`: 默认阴影
- `lg`: 大阴影
- `xl`: 特大阴影
- `2xl`: 双倍大阴影
- `none`: 无阴影

## 使用示例

```jsx
// 使用 CSS 变量映射的颜色
<div className="bg-primary-500 text-gray-900">
  主要色调背景
</div>

// 使用自定义阴影
<div className="shadow-lg">
  大阴影容器
</div>

// 使用自定义圆角
<button className="rounded-xl px-4 py-2">
  圆角按钮
</button>

// 使用自定义字体
<p className="font-mono text-sm">
  JetBrains Mono 代码字体
</p>
```

## 技术细节

### 配置方式
所有 CSS 变量通过 Tailwind 的 `var()` 语法引用，确保：
- 主题切换时自动更新
- 与设计系统 tokens.css 完全同步
- 减少了硬编码值的使用

### 兼容性
- 保持与现有 Tailwind 类的向后兼容
- 不影响原有的 Tailwind 默认值
- 仅在明确使用时覆盖默认配置

## 更新日志

- 2026-04-30: v1.14.1 发布 - Tailwind 配置增强
- 支持 CSS 变量映射、字体族、颜色、圆角、阴影配置