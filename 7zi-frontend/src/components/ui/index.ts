/**
 * UI Components
 *
 * Unified module for all UI components.
 * Currently includes:
 * - feedback: Loading states, skeleton loaders, toast notifications, error boundaries
 *
 * @example
 * import { LoadingState, useToast, ErrorBoundary } from '@/components/ui';
 */

// Feedback Components
export * from './feedback';
